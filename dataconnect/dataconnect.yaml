specVersion: "v1beta"
serviceId: "angular-stripe"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "angular-stripe-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
